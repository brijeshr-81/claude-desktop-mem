{
  "name": "claude-memory-mcp",
  "version": "1.0.0",
  "description": "MCP server for persistent memory via Obsidian vault markdown files",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
